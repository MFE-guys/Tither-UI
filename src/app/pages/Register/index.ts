import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'page-register',
  standalone: true,
  imports: [RouterModule],
  template: `<h1>Hello register decimate here!</h1>`
})
export class RegisterComponent {}
